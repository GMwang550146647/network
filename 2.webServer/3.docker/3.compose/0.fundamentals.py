"""
详见https://docs.docker.com/compose/gettingstarted/
1.运行
    docker-compose up
    docker-compose up -d [后台运行]
2.只运行其中的一个
    docker-compose run web env
3.代码补全
    brew install bash-completion

4.关闭服务
    docker-compose down -v 【-v 删除文件映射】
"""